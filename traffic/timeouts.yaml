apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: order-timeouts
  namespace: microservices
spec:
  hosts:
    - order-service
  http:
    - timeout: 3s
      route:
        - destination:
            host: order-service
